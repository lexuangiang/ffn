<%= link_to t("category_rates.new"), new_admin_category_match_rate_path %>
<div class="row">
  <% @categories.each do |category| %>
    <div class="col-sm-3 col-md-4">
      <%= link_to category.category_rates, admin_category_match_rate_path(category) %>
    </div>
  <% end %>
  <%= will_paginate @categories %>
</div>
